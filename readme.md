The source files for the Modern Software Engineering Enterprise paper.

To build run:

  $ ./build.sh

It'll spit out a new pdf file in the source directory.

Requires:
===

pandoc (which has some depends itself)
